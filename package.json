{
  "name": "x",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo with Rust crates and JS/TS packages",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "packageManager": "pnpm@10.10.0"
}